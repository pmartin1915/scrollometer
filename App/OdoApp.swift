import SwiftUI

@main
struct OdoApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}

struct ContentView: View {
    var body: some View {
        Text("Scrollometer — tracking not configured")
    }
}
