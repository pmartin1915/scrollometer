# IDEAS (append-only)

- 2026-08-18 — **v2 social competition**: friends/leaderboards competing for LOW mileage per app and overall (Perry's idea at planning). Deferred: needs a backend (accounts, sync); v1 is deliberately on-device-only with no data collection — adding a backend changes the privacy story and entitlement framing, so design it as an opt-in layer post-launch. Applies after v1 ships and retention is proven.
- 2026-08-18 — **Optional ReportExtension "compare with Apple's numbers" screen** (from feasibility research): display-only DeviceActivityReport view beside our estimate as a credibility asset. Deferred to M4; cut if it complicates the entitlement request.
- 2026-08-18 — **Android port opportunity** (from research): Android's UsageStatsManager gives real per-app time directly (no sandbox), so an Android version could be MORE accurate than iOS. Applies only if iOS version finds traction.
